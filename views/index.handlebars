<div class = "container">
    <div class = "row">
        <div class="col-md-12">
            <img src= "/assets/css/img/burger.jpg" alt= "burger" class= "img-responsiv center-block">
        </div>
    <!-- end first row-->
    </div>
        
    <div class="row">

		<div class="col-md-12">
			<h1 class="text-center text-danger">Eat-Da-Burger</h1>
		</div>
    <!--end second row-->
    </div>

    
    <!--Burger List before being eaten-->
    <div class = "container">
    <div class="row">
    
    <div class = "col-md-5">
            <h2>Uneaten Burgers</h2>
            
                {{#each burger_data}}
                    {{#unless this.devoured}}
               
                        <h3>{{this.id}}. {{this.burger_name}}
                
                            <form method= "POST" action= "/burger/update?_method=PUT" class = "button-size">
                            <input type = "hidden" name = "burger_id" type= "text" value= {{this.id}}>
                            <br>

                            <button type= "submit" class= "btn btn-default"> STUFF YOUR FACE!
                            </button>
                            </form>

                        <h3>
                        
                    {{/unless}}
                {{/each}}
            
    </div>
    </div>
    </div>
        <div class="col-md-2"></div>

        <div class ="col-md-5">
            <h2 class="text-muted">Eaten Burgers</h2>
			

              {{#each burger_data}}
                    {{#if this.devoured}}
                       
                        <h3 class="text-muted">{{this.id}}. {{this.burger_name}}</h3>
                        
                    {{/if}}
                {{/each}}
            
        </div>
    </div>
    <br><br>

    <div class = "container">
    <div class = "row">
        <div class ="col-md-12 text-center">
        <form action= "/burger/create" method="POST">
            <input tpe= "text" name="burger_name" class= "form-control"
            value= "" id= "enter_text">
            <button type= "submit" class= "btn btn-primary" id="
            text-enter-button"> Submit Your Burger Creation</button>
        </form>
        </div>
    </div>
</div>
</div>